# Setri — 配电网业扩智能评审副驾驶 (Co-Reviewer)

> 双引擎协作规则以 [AGEND.md](./AGEND.md) 为准；本文件仅负责项目背景、阶段信息、关键文件与项目约定。

## 项目概述

为上海经研院配电网评审专家构建的智能辅助评审系统，以 10kV 开关站为切入点，通过"规范库→文件整理→专家知识库→交叉提炼→自动审查"五站数据管道，实现从原始资料到结构化审查意见的端到端自动化。系统采用"智能体协同"架构，覆盖预审、评审、收口三大环节及 AI 智能问答功能。

## 快速启动

### 环境准备
```bash
# 安装 Python 依赖
uv sync

# 或使用传统方式
uv venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
uv pip install -e ".[llm,dev]"
```

### 开发命令
```bash
# 运行 CLI 工具
uv run setri --help

# 文件整理（P2）
uv run python scripts/reorganize_files.py
uv run python scripts/verify_reorganization.py

# 规范库提取（P1）
# （待实现）

# 运行测试
uv run pytest

# 代码检查
uv run ruff check src/
uv run ruff format src/
```

### 前端开发（待实现）
```bash
cd web
npm install
npm run dev  # 启动开发服务器
```

## 当前阶段

**统一交付演示系统重构与实现 — PRD v1.1**

PRD 已迭代至 v1.1，新增配套文档体系，当前主要任务：

- 重构计划：推进 monorepo 清场与新工程基线建立（阶段 A-G）
- P1 规范库构建：基于重构后的新管道实现
- P2 文件整理：基于重构后的新管道实现
- M2 技经资料预审：基于稳定资料集（可研/初设报告、概算/估算书、图纸/材料清册）实现字段抽取与基础一致性校验
- P3-P5：仅产品骨架页与扩展位预留
- M5 联调验收：按 Milestone 计划（M1-M5，18周周期）推进

**产品结构**：公共底座 + 核心主线（M1 业扩评审、M2 技经评审）+ 专题扩展（M3 开关站知识库、M4 进度管控、M5 竣工核查）

**科技项目群**（4 个在研项目驱动系统能力建设）：

| 项目 | 周期 | 合作方 | 经费 | 核心产出 |
|------|------|--------|------|----------|
| 25年 知识图谱评审规则自动化匹配 | 2025.8-12 | 上海交大 | 16.995万 | 知识图谱原型、匹配算法、发明专利1项 |
| 26年 智能体协同智能评审 | 2026.5-2027.11 | 上海交大 | 134.96万 | 软件原型1套、发明专利5项、论文1篇 |
| 26年 智慧进度管控 | 2026.1-2027.9 | 上海交大、上海电力大学 | 45万 | 技术报告2份、发明专利2件、软件原型1套 |
| 26年 竣工资料智能核查 | 2026.1-2027.9 | 上海交大、上海电力大学 | — | 技术报告2份、发明专利2件、软件原型1套 |

**管道依赖**：P1 独立 | P2 独立 | P3 依赖 P2 | P4 = P1 ∩ P3 | P5 依赖 P4

**首期输入方式**：以平台预注册样例项目/案例选择为主，不以浏览器内新增真实资料包上传为硬验收前提

## 关键文件

| 路径 | 说明 |
|------|------|
| `docs/prd.md` | 产品需求文档（当前 v1.1，统一交付演示系统主线） |
| `docs/统一交付演示系统需求文档.md` | 需求文档（v0.3），8 份合同材料归并为统一系统能力 |
| `docs/统一交付演示系统重构计划.md` | 工程重构计划（v0.6），monorepo 清场与新工程基线建立 |
| `docs/统一交付演示系统-Milestone计划.md` | 里程碑计划（v0.3），M1-M5 共 18 周周期 |
| `docs/合同承接矩阵.md` | 合同承接矩阵（v1.1），追踪 8 份合同在系统中的落位 |
| `docs/file_checklist.md` | 文件完整性清单 |
| `docs/architecture/` | P3-P5 方法层设计文档（多模态审核、专家知识库、评测底座） |
| `docs/archive/` | 历史分析归档（含旧 PRD、分析文档、旧开发计划等） |
| `智能评审里程碑计划【提纲模板】.docx` | 里程碑计划提纲（按经研院模板格式，含项目推进与成果创优） |
| `技术规范文件/` | 原始技术规范 PDF（~15 份，P1 数据源） |
| `项目测试数据/` | 3 个项目的送审版/审定版原始数据 |
| `data/projects/` | 标准化项目目录（P2 产出） |
| `data/regulations/` | 规范库（P1 产出，待构建） |
| `data/knowledge/` | 知识库与规则库（P3/P4 产出，待构建） |
| `apps/web/` | 前端工作台（待实现：Next.js + React + shadcn/ui） |
| `apps/api/` | 后端 HTTP API（待实现：FastAPI） |
| `apps/worker/` | 后台任务执行器（待实现：Celery + Redis） |
| `packages/domain/` | 领域模型、schema、共享类型（待建立） |
| `packages/pipelines/` | P1/P2 管道逻辑（待建立） |
| `packages/connectors/` | 文件系统/VLM/队列适配层（待建立） |
| `.claude/skills/file-reorganizer/SKILL.md` | 文件整理 Skill（含同义词映射表） |

## 合同材料

仓库根目录存放 8 份项目合同/可研报告原件，对应合同承接矩阵中的 8 个项目：

| 文件 | 对应项目 | 状态 |
|------|----------|------|
| `2024年技术服务合同_配电网业扩项目智能化审查与合规性评估.pdf` | M1 业扩评审 | 已验收 |
| `2025年_基于知识图谱的配电网业扩项目评审规则自动化匹配研究【导出】(1).pdf` | M1 规则匹配 | 已验收 |
| `2025年_1、基于电力行业大模型的配网业扩技经资料智能辅助预审方法研究和技术验证.pdf` | M2 技经预审 | 执行中 |
| `2025年_2、基于配网业扩开关站专家知识库构建开关站辅助评审方法研究与技术验证.pdf` | M3 知识库 | 已验收 |
| `2025年_3、基于知识图谱的配网业扩技经智能辅助评审与优化方法研究.pdf` | M2 技经评审 | 执行中 |
| `基于"智能体协同"的配电网业扩项目智能评审关键技术研究与应用(1).pdf` | M1 智能体协同（26年，134.96万） | 执行中 |
| `附件3…智慧进度管控方法与验证）.doc` | M4 进度管控（26年，45万） | 招标中 |
| `附件3…智能核查方法研究）.doc` | M5 竣工核查（26年） | 招标中 |

## 工作约定

- 三大文档协同维护：
  - PRD 负责产品定义、能力边界、模块组织
  - 重构计划负责工程结构、技术实现、版本规划
  - Milestone 计划负责交付节奏、里程碑目标、验收标准
- 需求/重构/Milestone 文档修改后需同步更新版本号与变更记录
- 新的数据分析结论写入 `docs/architecture/` 目录，JSON 格式；早期分析文档统一归档到 `docs/archive/`
- 系统输入以"送审版"为基准（详见 PRD §4）
- 文件类型识别需支持模糊匹配，容忍命名不一致（详见 PRD §8.4）
- 目录命名统一使用中文

## 标准化目录结构（PRD v0.7）

```
data/projects/{project_id}/
├── meta.json                          # 项目元数据
├── 过程管理/                           # 过程管理文件
│   ├── 关键节点时间/
│   ├── 项目概况信息/
│   ├── 设计人员信息/
│   ├── 预审专家意见/
│   ├── 正式评审专家意见/
│   ├── 收口评审专家意见/
│   ├── 评审意见/
│   ├── 发文意见/
│   ├── 项目评审记录/
│   └── 评分文件/
├── 设计文件/
│   ├── 送审版/
│   │   ├── 项目级/                     # 共享文件（设计委托书、可研报告、投资汇总表等）
│   │   ├── 开关站/                     # 分项：工程图纸/ 建筑图纸/ 工程估算书/ 材料清册/
│   │   ├── 街坊站-N/
│   │   ├── 站内电缆/
│   │   ├── 高压电缆/
│   │   ├── 低压电缆/
│   │   ├── 通信/
│   │   └── 信息采集/
│   └── 审定版/                         # 结构同送审版
└── 解析结果/                           # P2 解析输出（JSON）
```

## 技术栈

- 后端：Python (FastAPI) + Celery + Redis
- 前端：React + Next.js + TypeScript + shadcn/ui + Tailwind CSS
- VLM：Gemini 3.0 Flash（MVP）→ Qwen VL（微调私有化）
- 数据库：PostgreSQL
- 认证：JWT（内网部署）
- 部署：内网本地服务器

## 代码组织

```
src/setri/
├── cli.py              # CLI 入口
├── config.py           # 配置管理
├── common/             # 公共工具
├── llm/                # LLM 集成
├── p1_regulations/     # P1 规范库构建
└── p2_reorganize/      # P2 文件整理

scripts/
├── reorganize_files.py         # 文件整理主脚本
├── verify_reorganization.py    # 整理结果验证
├── fix_file_reorganization.py  # 修复整理问题
└── cleanup_duplicates.py       # 清理重复文件

data/
├── projects/           # 标准化项目目录（P2 产出）
├── regulations/        # 规范库（P1 产出，待构建）
└── knowledge/          # 知识库与规则库（P3/P4 产出）
    └── 规则库/
        ├── source/     # Markdown 规则源文件
        ├── compiled/   # JSON 编译规则
        ├── templates/  # 规则模板
        └── versions/   # 规则版本历史

web/                    # 前端工作台（待实现）
server/                 # 后端 API（待实现）
```

## 开发注意事项

### 文件整理（P2）
- 使用 `.claude/skills/file-reorganizer/SKILL.md` 中的同义词映射表
- 文件类型识别支持模糊匹配，容忍命名不一致
- 目录命名统一使用中文
- 整理后使用 `verify_reorganization.py` 验证结果

### 规则库（P4）
- 规则源文件使用 Markdown（`data/knowledge/规则库/source/`）
- 编译后的 JSON 文件在 `compiled/` 目录
- 规则分为简单规则（结构化执行）和复杂规则（LLM 推理）
- 模板文件在 `templates/` 目录

### P1-P5 管道依赖
- **P1 独立**：规范库构建可独立执行
- **P2 独立**：文件整理可独立执行
- **P3 依赖 P2**：专家知识库需要整理后的项目文件
- **P4 = P1 ∩ P3**：规则提炼需要规范库和专家知识库
- **P5 依赖 P4**：自动审查需要规则库

### CLI 工具
- 使用 `uv run setri` 访问 CLI 命令
- 基于 Typer 构建，支持 `--help` 查看帮助

## 术语表

| 术语 | 含义 |
|------|------|
| 送审版 | 专家收到的待审材料（修改前），系统输入基准 |
| 审定版 | 专家评审后的修正版本（修改后），参考标准答案 |
| 三元关联 | 专家意见 ↔ 设计文件 ↔ 引用规范 的关联结构 |
| 管道站 | 数据管道中的一个处理阶段（P1-P5） |
| 交叉提炼 | "应然"（规范）× "实然"（专家）→ 可执行规则 |
| 智能体协同 | 多智能体系统（MAS）架构，信息抽取/审查/意见落实/总控智能体分工协作 |
| RAG | 检索增强生成，用于知识增强的智能问答 |
| 证据链 | 核查结论回溯到 file + page + snippet 的可解释输出 |
