# File-Reorganizer 项目文件标准化整理 Skill

## 触发条件

当用户提到以下关键词时激活本 Skill：
- 文件整理、文件归档、文件重组、reorganize files
- 标准化目录、存储规范
- file-reorganizer

## 概述

本 Skill 将 `项目测试数据/` 中散落的原始项目文件，按 PRD §4.3.8 存储规范整理到标准化目录结构 `data/projects/{project_id}/`。

核心原则：**Claude 负责语义匹配（目录名 → 标准分类），Bash cp 负责文件复制**。
- 不写 Python 脚本——核心难点是语义匹配，这是 Claude 的强项
- 复制而非移动，保留源文件不动
- 仅处理常见文件类型：pdf, xlsx, xls, docx, doc, pptx
- 跳过：.ofd, .bdd3, .bpz17, .DS_Store, .db 等

## 项目注册表

| short_name | project_id | full_name |
|------------|-----------|-----------|
| 崇明盛世新苑 | C109HZ22A062 | 上海崇明城桥置业高岛路170弄盛世新苑10kV普通住宅红线内接入工程 |
| 奉贤悦景名筑 | C1092322A133 | 上海奉贤翀溢置业庄良路60弄悦景名筑10kV普通住宅红线内接入工程 |
| 奉贤咉懿佳园 | C1092323N6S8 | 上海奉贤陕名置业正旭路80弄咉懿佳园10kV普通住宅红线内接入工程 |

## 源目录结构

```
项目测试数据/
├── 修改前/                          ← 送审版入口
│   └── 成果资料({full_name}（{project_id}）)/
│       └── 成果资料/
│           ├── [P-xx 根目录层文件夹]  ← 关键节点时间、项目概况信息...
│           └── 送审版/
│               ├── [A-xx 辅助文件]    ← 设计委托书、内审意见...
│               └── [专业子项目录]      ← KT站、PT站、电缆、通信...
└── 修改后/                          ← 审定版入口
    └── 成果资料({full_name}（{project_id}）)/
        └── 成果资料/
            ├── [P-xx 根目录层文件夹]
            └── 审定版/
                ├── [A-xx 辅助文件]
                └── [专业子项目录]
```

## 目标目录结构（§4.3.8）

```
data/projects/{project_id}/
├── meta.json
├── process/                           ← P-xx 过程管理文件
│   ├── P-01_milestones/
│   ├── P-02_project_info/
│   ├── P-03_designer_info/
│   ├── P-04_preliminary_review/
│   ├── P-05_formal_review/
│   ├── P-06_scoring/
│   ├── P-07_closeout_review/
│   ├── P-08_review_comments/
│   ├── P-09_issued_comments/
│   └── P-10_review_records/
└── design/
    ├── auxiliary/                     ← A-01~A-09 共享辅助文件（不区分版本）
    │   ├── A-01_design_commission/
    │   ├── A-02_internal_review/
    │   ├── A-03_other_references/
    │   ├── A-04_investment_summary/
    │   ├── A-05_financial_audit/
    │   ├── A-06_power_supply_review/
    │   ├── A-07_telecom_confirmation/
    │   ├── A-08_equipment_list/
    │   └── A-09_case_analysis/
    ├── submitted/                     ← 送审版
    │   ├── feasibility_report/        ← A-10（含各专业子项的说明书/可研报告）
    │   ├── drawings/                  ← D-xx 专业图纸（仅图纸类文件）
    │   │   ├── D-01_switchgear/
    │   │   ├── D-02_substation_01/  ... _NN/
    │   │   ├── D-03_station_cable/   ... _NN/（可能多个）
    │   │   ├── D-04_hv_cable/
    │   │   ├── D-05_lv_cable/
    │   │   ├── D-06_telecom/
    │   │   └── D-07_metering/
    │   ├── estimates/                 ← E-xx 工程估算
    │   │   ├── E-01_switchgear/
    │   │   ├── E-02_substation_01/  ... _NN/
    │   │   ├── E-03_station_cable/   ... _NN/
    │   │   ├── E-04_hv_cable/
    │   │   ├── E-05_lv_cable/
    │   │   ├── E-06_telecom/
    │   │   └── E-07_metering/
    │   ├── materials/                  ← 各专业子项的材料清册/材料表
    │   │   ├── D-01_switchgear/
    │   │   ├── D-02_substation_01/  ... _NN/
    │   │   └── ...（仅创建有内容的）
    │   ├── demolition/                ← 各专业子项的拆除设备清单
    │   │   ├── D-01_switchgear/
    │   │   └── ...（仅创建有内容的）
    │   └── references/                ← 各专业子项的依据文件
    │       └── ...（仅创建有内容的）
    └── approved/                      ← 审定版（结构同 submitted）
        ├── feasibility_report/
        ├── drawings/
        ├── estimates/
        ├── materials/
        ├── demolition/
        └── references/
```

**目录命名规范**：

- **P-xx 目录**：`P-{编号}_{英文名称}`，如 `P-01_milestones`
- **A-xx 目录**：`A-{编号}_{英文名称}`，如 `A-01_design_commission`
- **D-xx 目录**：`D-{编号}_{专业类型}`，如 `D-01_switchgear`、`D-02_substation_01`
- **E-xx 目录**：`E-{编号}_{专业类型}`，如 `E-01_switchgear`、`E-02_substation_01`
- **materials/demolition/references 子目录**：必须使用完整的 `D-xx_{专业类型}` 格式，不能使用简化编号

**专业类型映射表**：

| D-xx | 专业类型标识符 | 说明 |
|------|---------------|------|
| D-01 | switchgear | 开关站 |
| D-02 | substation_01, substation_02, ... | 街坊站（按编号） |
| D-03 | station_cable_01, station_cable_02, ... | 站内电缆（按编号） |
| D-04 | hv_cable | 高压电缆 |
| D-05 | lv_cable | 低压电缆 |
| D-06 | telecom | 通信 |
| D-07 | metering | 信息采集 |

## 同义词映射表（§4.3.6 精简版）

### 过程管理文件（P-xx）— 根目录层按名称直接匹配

| type_id | 标准名 | 匹配关键词 |
|---------|--------|-----------|
| P-01 | 关键节点时间 | 关键节点 |
| P-02 | 项目概况信息 | 项目概况 |
| P-03 | 设计人员信息 | 设计人员 |
| P-04 | 预审专家意见 | 预审 |
| P-05 | 正式评审专家意见 | 正式评审 |
| P-06 | 评分文件 | 评分 |
| P-07 | 收口评审专家意见 | 收口 |
| P-08 | 评审意见 | 评审意见（不含"预审""正式""收口"） |
| P-09 | 发文意见 | 发文意见, 发文意见（WORD版） |
| P-10 | 项目评审记录 | 评审记录 |

### 共享辅助文件（A-xx）— 送审版/审定版内的非专业目录

| type_id | 标准名 | 匹配关键词 |
|---------|--------|-----------|
| A-01 | 设计委托书 | 设计委托书, 设计中标通知书或委托书, 设计中标通知书 |
| A-02 | 内审意见 | 内审意见, 内审意见及签到单 |
| A-03 | 其他依据 | 其他依据, 其它依据, 其他 |
| A-04 | 投资汇总表 | 投资汇总 |
| A-05 | 财务审核表 | 财务审核 |
| A-06 | 供电方案审核单 | 供电方案 |
| A-07 | 通信方案确认单 | 通信方案 |
| A-08 | 主要设备材料清册 | 主要设备材料清册, 主要设备材料清册（pdf、word版） |
| A-09 | 案例分析报告 | 案例分析 |
| A-10 | 可研报告 | 可研报告, 可行性研究报告, 初设报告, 设计报告, 可研报告批复 |

### 专业图纸（D-xx）— 从长目录名中提取专业关键词

| type_id | 标准名 | 匹配关键词 |
|---------|--------|-----------|
| D-01 | 开关站 | KT站, 开关站电气一次, 开关站 |
| D-02 | 街坊站 | PT-x站, PML, 街坊站电气一次, 零x街坊站 |
| D-03 | 站内电缆 | 站内电缆, -10kV站内电缆 |
| D-04 | 高压电缆 | 内线电缆-10kV, 10kV电缆, （10kV电缆） |
| D-05 | 低压电缆 | 内线电缆-0.4kV, 0.4kV电缆, （0.4kV电缆） |
| D-06 | 通信 | 内线通信, 通信, （通信） |
| D-07 | 信息采集 | 信息采集, 用电信息采集, （信息采集） |

### 工程估算（E-xx）— 在各专业目录内部找"工程估算书/工程概算书"子目录

| type_id | 标准名 | 说明 |
|---------|--------|------|
| E-01 | 开关站工程估算 | D-01 目录内的 工程估算书/工程概算书 子目录 |
| E-02 | 街坊站工程估算 | D-02 目录内的 工程估算书/工程概算书 子目录 |
| E-03 | 站内电缆工程估算 | D-03 目录内的 工程估算书/工程概算书 子目录 |
| E-04 | 高压电缆工程估算 | D-04 目录内的 工程估算书/工程概算书 子目录 |
| E-05 | 低压电缆工程估算 | D-05 目录内的 工程估算书/工程概算书 子目录 |
| E-06 | 通信工程估算 | D-06 目录内的 工程估算书/工程概算书 子目录 |
| E-07 | 信息采集工程估算 | D-07 目录内的 工程估算书/工程概算书 子目录 |

> **工程估算匹配关键词**：工程估算书, 工程概算书, 工程估算书(PDF版), 工程概算书（PDF版）
> **注意**：跳过"工程估算书（软件版）"及 .bpz17 文件

### 非图纸文件（从专业子项中分离）— 与 drawings/ 平级的独立目录

| 目录名 | 匹配关键词（子目录名或文件名） |
|--------|-------------------------------|
| materials | 主要建设材料表, 主要材料清册, 设备材料清册, 设备材料清册（PDF版）, 材料表, 材料表.pdf |
| demolition | 拆除设备清单, 拆除设备材料清单 |
| references | 依据文件 |
| feasibility_report | 可研报告, 可行性研究报告, 初设报告, 设计报告, 说明书（内容为可研报告） |

> **图纸类子目录（留在 drawings/）**：其他图纸, 土建总平面及竖向布置图, 电缆通道断面图, 图纸资料
> **说明书判断规则**：文件名含"可研报告"→ 归入 `feasibility_report/`；文件名含"情况说明"或其他非可研内容 → 归入 `references/`

## 匹配优先级与歧义处理

1. **D-03 vs D-04 歧义**：目录名含"10kV站内电缆"时归 D-03（站内电缆），含"10kV电缆"但不含"站内"时归 D-04（高压电缆）
2. **D-01 vs D-02 歧义**：含"开关站"归 D-01，含"街坊站/PT/PML"归 D-02
3. **P-08 vs P-04/P-05/P-07**：P-08 仅匹配不含"预审""正式""收口"修饰词的"评审意见"
4. **P-09 合并**：`发文意见` 和 `发文意见（WORD版）` 的文件都复制到 P-09
5. **A-10 可研报告**：可能散落在各专业子项下（信息采集、KT站等），需去重归并到 A-10
6. **街坊站编号**：多个街坊站按原始目录中的编号顺序映射为 D-02_01, D-02_02...（E-02 同理）
7. **站内电缆编号**：多个站内电缆按原始目录中的编号顺序映射为 D-03_01, D-03_02...（E-03 同理）

## 文件类型过滤

**包含**（不区分大小写）：
```
.pdf .xlsx .xls .docx .doc .pptx .ofd
```

**排除**：
```
.bdd3 .bpz17 .DS_Store .db .tmp
```

---

## Phase 0: 参数确认

与用户确认以下参数：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| source_dir | `项目测试数据/` | 原始数据根目录 |
| target_dir | `data/projects/` | 标准化输出根目录 |
| projects | 全部 3 个 | 要处理的项目（可选单个） |

确认后进入 Phase 1。

## Phase 1: 扫描 & 语义映射

对每个项目，执行以下步骤：

### 1a. 定位源目录

根据项目注册表，构造源路径：
- 送审版根：`{source_dir}/修改前/成果资料({full_name}（{project_id}）)/成果资料/`
- 审定版根：`{source_dir}/修改后/成果资料({full_name}（{project_id}）)/成果资料/`

用 `ls` 列出两个根目录的一级子目录。

### 1b. 映射根目录层（P-xx）

扫描送审版根目录（不含"送审版"本身），将每个一级子目录映射到 P-xx：
- 使用上方同义词映射表进行语义匹配
- `发文意见` 和 `发文意见（WORD版）` 都映射到 P-09
- 审定版根目录的 P-xx 文件夹内容也复制到同一 P-xx 目标（合并）

### 1c. 映射送审版内部（A-xx + D-xx + E-xx + 非图纸文件）

扫描 `送审版/` 下的一级子目录：

1. **辅助文件识别**：短名称目录（不含项目全名前缀）→ 匹配 A-01~A-09
2. **专业子项识别**：长名称目录（含项目全名前缀）→ 提取括号/空格后的专业关键词 → 匹配 D-01~D-07
3. **工程估算提取**：进入每个 D-xx 匹配的专业目录，查找名为"工程估算书*"或"工程概算书*"的子目录 → 映射到对应 E-xx
4. **可研报告归并**：在各专业子项内查找"可研报告/可行性研究报告/初设报告/设计报告/说明书"子目录 → 全部归并到 `feasibility_report/`
5. **材料清册提取**：在各专业子项内查找"主要建设材料表/主要材料清册/设备材料清册/材料表"子目录或文件 → 映射到 `materials/D-xx_{专业类型}/`（如 `materials/D-01_switchgear/`、`materials/D-02_substation_01/`）
6. **拆除设备材料提取**：在各专业子项内查找"拆除设备清单/拆除设备材料清单"子目录 → 映射到 `demolition/D-xx_{专业类型}/`（如 `demolition/D-04_hv_cable/`）
7. **依据文件提取**：在各专业子项内查找"依据文件"子目录 → 映射到 `references/D-xx_{专业类型}/`（如 `references/D-01_switchgear/`）
8. **主要设备材料清册**：可能出现在送审版一级（如项目3的"主要设备材料清册（pdf、word版）"）→ 映射到 A-08

**重要**：materials/demolition/references 子目录必须使用完整的 `D-xx_{专业类型}` 格式，专业类型标识符与对应的 drawings/D-xx 目录保持一致。

> **说明书特殊处理**：经验证，各专业子项下的"说明书"目录实际存放的是可研报告 PDF，应归入 `feasibility_report/`。但需注意个别目录可能混入其他文件（如"情况说明"），非可研报告的文件应归入 `references/`。

### 1d. 映射审定版内部

同 1c 逻辑，扫描 `审定版/` 下的目录。
- A-01~A-09 辅助文件：与送审版共享，仅当送审版中未出现时才从审定版补充
- 可研报告、D-xx、E-xx、材料清册、拆除设备材料、依据文件：独立映射到 approved/ 下

### 1e. 生成映射计划

输出结构化映射表，格式：

```
映射计划 — {short_name} ({project_id})
============================================
| # | 源路径 | → | 目标路径 | 文件数 | 备注 |
|---|--------|---|---------|--------|------|
| 1 | .../送审版/设计中标通知书或委托书/ | → | design/auxiliary/A-01_design_commission/ | 3 | |
| 2 | .../送审版/...（KT站）/其他图纸/ | → | design/submitted/drawings/D-01_switchgear/ | 5 | |
| ... | | | | | |
```

标注：
- `[未匹配]`：无法映射的目录
- `[歧义]`：可能匹配多个分类的目录
- `[跳过]`：仅含排除文件类型的目录

## Phase 2: 用户审核映射计划

将 Phase 1 的映射表展示给用户，逐项目确认：

1. 展示每个项目的映射表（Phase 1e 格式）
2. 高亮标注 `[未匹配]`、`[歧义]`、`[跳过]` 的条目
3. 统计摘要：已匹配 N 个目录，未匹配 M 个，跳过 K 个
4. 询问用户是否需要调整映射

用户确认后进入 Phase 3。如有调整，修改映射表后重新确认。

---

## Phase 3: 执行文件复制

### 3a. 创建目录结构

对每个项目，用 `mkdir -p` 创建完整目录树：

```bash
PROJECT_DIR="{target_dir}/{project_id}"

# 根目录（不含 original/）
mkdir -p "$PROJECT_DIR"/{process,design/{auxiliary,submitted/{feasibility_report,drawings,estimates,materials,demolition,references},approved/{feasibility_report,drawings,estimates,materials,demolition,references}}}

# P-xx（仅创建有内容的）
mkdir -p "$PROJECT_DIR/process/P-01_milestones"
# ... 按映射表创建

# A-xx（仅创建有内容的）
mkdir -p "$PROJECT_DIR/design/auxiliary/A-01_design_commission"
# ... 按映射表创建

# D-xx / E-xx / materials / demolition / references（仅创建有内容的）
# 注意：materials/demolition/references 子目录必须使用完整的 D-xx_{专业类型} 格式
mkdir -p "$PROJECT_DIR/design/submitted/drawings/D-01_switchgear"
mkdir -p "$PROJECT_DIR/design/submitted/estimates/E-01_switchgear"
mkdir -p "$PROJECT_DIR/design/submitted/materials/D-01_switchgear"
mkdir -p "$PROJECT_DIR/design/submitted/demolition/D-01_switchgear"
mkdir -p "$PROJECT_DIR/design/submitted/references/D-01_switchgear"

# 街坊站示例（多个编号）
mkdir -p "$PROJECT_DIR/design/submitted/drawings/D-02_substation_01"
mkdir -p "$PROJECT_DIR/design/submitted/estimates/E-02_substation_01"
mkdir -p "$PROJECT_DIR/design/submitted/materials/D-02_substation_01"
mkdir -p "$PROJECT_DIR/design/submitted/demolition/D-02_substation_01"

# ... 按映射表创建其他目录
```

### 3b. 按映射表复制分类文件

遍历映射表，对每条映射执行：

```bash
# 仅复制常见文件类型，排除子目录中已单独映射的内容
find "{源路径}" -maxdepth 1 -type f \( \
  -iname "*.pdf" -o -iname "*.xlsx" -o -iname "*.xls" \
  -o -iname "*.docx" -o -iname "*.doc" -o -iname "*.pptx" -o -iname "*.ofd" \
\) -exec cp {} "{目标路径}/" \;
```

**特殊处理**：
- **E-xx 提取**：从 D-xx 对应的专业目录中，找到"工程估算书"/"工程概算书"子目录，将其中的常见文件复制到 E-xx 目标
- **D-xx 图纸**：复制专业目录下的图纸类子目录（其他图纸、土建总平面及竖向布置图、电缆通道断面图、图纸资料）中的常见文件，以及专业目录一级下的散落图纸文件。排除已单独映射的子目录（工程估算书、可研报告、说明书、材料清册、拆除设备清单、依据文件）
- **可研报告归并去重**：多个专业子项下的"可研报告"和"说明书"目录可能包含同一份文件，按文件名去重后复制到 `feasibility_report/`。说明书中的非可研报告文件（如"情况说明"）归入 `references/`
- **材料清册提取**：从各专业目录中找到"主要建设材料表/主要材料清册/设备材料清册/材料表"子目录或同名文件 → 复制到 `materials/D-xx_{专业类型}/`。专业类型标识符必须与该专业对应的 drawings/D-xx 目录保持一致（如 D-01 对应 switchgear，D-02_01 对应 substation_01）
- **拆除设备材料提取**：从各专业目录中找到"拆除设备清单/拆除设备材料清单"子目录 → 复制到 `demolition/D-xx_{专业类型}/`。专业类型标识符规则同上
- **依据文件提取**：从各专业目录中找到"依据文件"子目录 → 复制到 `references/D-xx_{专业类型}/`。专业类型标识符规则同上

**目录命名示例**：
- 开关站材料清册：`materials/D-01_switchgear/`
- 街坊站01拆除清单：`demolition/D-02_substation_01/`
- 高压电缆依据文件：`references/D-04_hv_cable/`

### 3c. 执行顺序

按项目逐个执行，每个项目内按以下顺序：
1. mkdir -p 创建目录
2. 复制 P-xx
3. 复制 A-01~A-09（从送审版提取，审定版补充）
4. 复制 feasibility_report submitted/approved（含说明书中的可研报告）
5. 复制 D-xx submitted/approved（仅图纸类文件）
6. 复制 E-xx submitted/approved
7. 复制 materials submitted/approved
8. 复制 demolition submitted/approved
9. 复制 references submitted/approved

每个项目完成后输出简要进度。

## Phase 4: 生成 meta.json & 验证

### 4a. 生成 meta.json

为每个项目在 `{target_dir}/{project_id}/meta.json` 写入：

```json
{
  "project_id": "C109HZ22A062",
  "project_name": "上海崇明城桥置业高岛路170弄盛世新苑10kV普通住宅红线内接入工程",
  "short_name": "崇明盛世新苑",
  "imported_at": "2026-03-01T12:00:00+08:00",
  "source_paths": {
    "submitted": "项目测试数据/修改前/成果资料(...)/成果资料/",
    "approved": "项目测试数据/修改后/成果资料(...)/成果资料/"
  },
  "file_type_filter": ["pdf", "xlsx", "xls", "docx", "doc", "pptx", "ofd"],
  "path_mapping": [
    {
      "source": "送审版/设计中标通知书或委托书/",
      "target": "design/auxiliary/A-01_design_commission/",
      "type_id": "A-01",
      "file_count": 3
    }
  ]
}
```

`path_mapping` 数组记录 Phase 1 的完整映射关系，支持双向追溯。

### 4b. 验证

对每个项目执行验证：

1. **文件计数对比**：统计源目录中常见文件类型的总数 vs 目标目录中的文件总数
2. **分类覆盖检查**：列出每个分类目录下的文件数量，标注空目录

输出验证报告：

```
验证报告 — 崇明盛世新苑 (C109HZ22A062)
==========================================
源文件总数（常见类型）：  128
分类文件总数：            125
差异：                    3（已标注原因）

分类覆盖：
  P-01~P-10:  ✅ 8/10 有内容
  A-01~A-09:  ✅ 6/9 有内容
  feasibility_report:    ✅ submitted + approved
  D-01~D-07:  ✅ 7/7 有内容（submitted）
  E-01~E-07:  ✅ 7/7 有内容（submitted）
  materials:    ✅ 5/7 有内容（submitted）
  demolition: ✅ 4/7 有内容（submitted）
  references:    ✅ 1/7 有内容（submitted）
```

### 4c. 完成

所有项目验证通过后，输出总结：
- 处理了 N 个项目
- 总文件数
- 未匹配/跳过的目录清单（如有）
- 建议用户检查 `[未匹配]` 条目

---

## 已知限制

1. **语义匹配非 100%**：极端命名变体可能需要人工干预（Phase 2 审核环节兜底）
2. **文件去重**：可研报告去重基于文件名，不做内容级比对
3. **街坊站排序**：多个街坊站的编号顺序基于目录名排序，可能与实际编号不完全一致
4. **说明书内容判断**：说明书目录中绝大多数为可研报告，但个别可能混入其他文件（如情况说明），需在 Phase 2 审核时关注
5. **目录命名验证**：执行完成后，应验证 materials/demolition/references 子目录是否使用了完整的 `D-xx_{专业类型}` 格式。可使用以下命令检查：
   ```bash
   # 查找所有使用简化编号的错误目录（应返回 0）
   find data/projects -type d -regex ".*/\(materials\|demolition\|references\)/D-0[1-7]$" | wc -l
   ```
